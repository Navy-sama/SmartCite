/*// import { create } from 'zustand';

export type Signalement = {
  id: string;
  image: string;
  title: string;
  category: string;
  description: string;
  location: string;
  status: 'pending' | 'in treatment' | 'resolved';
  created_at: string;
  priority: number;
};

type Store = {
  reports: Signalement[];
};

export const useSignalementStore = create<Store>(() => ({
  reports: [],
}));*/
